# Skybridge Environment Variables
# Copie para .env e configure conforme necessário

# API
SKYBRIDGE_HOST=0.0.0.0
SKYBRIDGE_PORT=8000
SKYBRIDGE_LOG_LEVEL=INFO
SKYBRIDGE_DEBUG=false

# Ngrok (opcional)
# Veja PB002 para configurar URL fixa
NGROK_ENABLED=false
# NGROK_AUTH_TOKEN=your_token_here  # Obter em https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_DOMAIN=skybridge-dev.ngrok-free.app  # Domínio reservado (opcional, mas recomendado)

# FileOps - Allowlist de segurança
# Modo: "dev" (codebase) ou "production" (workspace)
FILEOPS_ALLOWLIST_MODE=dev
# FILEOPS_DEV_ROOT=B:\_repositorios\skybridge  # Opcional, default é cwd
# FILEOPS_PROD_ROOT=\workspace  # Default para produção

# Webhooks - Branch base para worktrees de agentes
# Branch usada como base para criar worktrees isolados para processar issues
# Padrão: "dev" (ou "main" dependendo do seu fluxo)
WEBHOOK_BASE_BRANCH=dev


# Security baseline (JSON-RPC)
# API key global (fase 1)
# SKYBRIDGE_API_KEY=change-me
# Map de chaves por client_id (opcional): key1:clientA;key2:clientB
# SKYBRIDGE_API_KEYS=key1:clientA;key2:clientB

# Policy por method: clientA:health,fileops.read,rpc.discover;clientB:health
# SKYBRIDGE_METHOD_POLICY=clientA:health,fileops.read,rpc.discover

# Rate limit por client_id (requests/min). 0 desativa.
SKYBRIDGE_RATE_LIMIT_PER_MINUTE=0
# Bypass de auth para localhost (testes)
ALLOW_LOCALHOST=false
# Allowlist de IPs (opcional)
# SKYBRIDGE_IP_ALLOWLIST=203.0.113.10,198.51.100.5

# Bearer tokens (futuro): token1:clientA;token2:clientB
# SKYBRIDGE_BEARER_ENABLED=false
# SKYBRIDGE_BEARER_TOKENS=token1:clientA

# HTTPS (TLS)
# SKYBRIDGE_SSL_ENABLED=false
# SKYBRIDGE_SSL_CERT_FILE=path/to/cert.pem
# SKYBRIDGE_SSL_KEY_FILE=path/to/key.pem
